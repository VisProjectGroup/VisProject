#import "template.typ" : colab, solution

#show: colab.with(
  instructor: "", 
  class: "",
  ta: "",
  author: "", 
  lab_name: "",
  author-id: "", 
)

= 设计需求

== 背景介绍



== 用户群体


== 需求分析



= 设计介绍

== 数据处理与生成

=== 数据获取

==== 地图数据获取

在#link("https://datav.aliyun.com/portal/school/atlas/area_selector", "阿里云地图下载工具")中获取中国按照省级行政区和地级市级行政区的分划的两组边界数据。



==== 空气质量数据获取


==== 天气数据获取


==== 工业能耗数据获取



=== 数据清洗



==== 空气质量数据清洗

==== 天气数据清洗


==== 工业能耗数据清洗

=== 数据转换

==== 月度数据



==== 年度数据


==== 地图数据修改

将北京、天津、上海和重庆四个直辖市的所有区的边界点删除，并使用中国省界地图的四个地级市的数据带入，修改完成。


==== 柱状图数据修改

使用 python 脚本读取 csv 文件，将之按照地级市打包为“地级市名.json”将数据转换为 json 文件，并在前端读取 json 文件进行渲染。

==== 热力图数据修改

继承年度数据，用于计算热力图中的皮尔逊相关系数。



== 整体布局





== 视图介绍

=== 地图

中间部分“中国地图”的文字下是

地图视图具有两种模式：省份配色与 AQI 配色。

#figure(
  image("assets/map_province.png", width: 50%),
    caption: [
    省份配色
  ],
)<fig:map_province>

省份配色模式：在地图上显示各省份的颜色，便于区分各个省份的行政区划。

#figure(
  image("assets/map_aqi.png", width: 50%),
  caption: [
    AQI 配色
  ],
)<fig:map_aqi>

AQI 配色模式：在地图上显示各个城市的 AQI 值，从高到低用红-黄-绿的颜色渐变，颜色越红表示 AQI 值越高，越绿表示 AQI 值越低，便于观察各个城市的空气质量情况。左下角图例展示了各个颜色对应的 AQI 值范围。我们可以通过颜色快速了解到各个城市的空气质量情况。其中数据缺失的部分用灰色表示。

#figure(
  image("assets/map_botton.png", width: 50%),
  caption: [
    切换按钮
  ],
)<fig:map_botton>

地图可以通过右上角的按钮进行切换。可以通过左下角的滚轮选取 AQI 在某个范围内的城市。

地图可以缩放、拖动。鼠标悬停到某个市，可以用对话框显示该市的名称、AQI 值、省级行政区名称。

地图的边界数据储存在 assets/map/china.json 文件中，通过读取 json 文件，在前端渲染地图。使用 Map.vue 组件调用 echart 绘制地图实现。点击某个城市，会与发送这个城市名称的信号绑定。

=== 柱状图

#figure(
  image("assets/colum.png", width: 50%),
  caption: [
    柱状图
  ],
)<fig:colum>

柱状图的图表展示了 AQI、降水量、风速和工业能耗四组数据。可以通过左侧侧边栏选择柱状图表示的数据，可以通过右上方的日期选择器控件选择需要显示的日期范围，日期范围是 2015-2021。通过观察具体日期的柱状图，可以了解到不同日期的 AQI、降水量、风速和工业能耗的变化趋势。

可以点击地图视图的某个城市，显示该城市的详细信息。

=== 热力图

#figure(
  image("assets/heatmap.png", width: 50%),

  caption: [
    热力图
  ],
)<fig:heatmap>

热力图视图包含了 AQI、降水量、风速和工业能耗四组数据的相关性，可以直观地了解数据之间的相关性，使用皮尔逊相关系数来表示相关性，使用红-黄-蓝的颜色渐变来表示相关性强度。1 表示完全相关，使用红色，-1 表示完全不相关，使用蓝色，0 表示无相关性，使用浅黄色。

#figure(
    image("assets/heatmap2.png", width: 50%),
    caption: [
        热力图选择器
    ],
)<fig:heatmap2>

热力图可以使用左下角的选择器选择显示全图还是三角形区域。可以通过右下角选择器选择使用什么色系染色。通过地图视图点击城市可以切换各个城市的视图。

=== 玫瑰图

玫瑰图包含了雨量-AQI 和风速-AQI 两组数据，使用了极坐标系展示了一年的逐月情况，可以通过按钮切换不同年份。AQI 数据使用极坐标系中边缘平滑的区域来展示，风力、降水量数据使用极坐标系的折线来展示。

鼠标悬浮在图中某月的扇形区域时，会在图的下方显示这个月 AQI、降水量或风速的具体数值。

=== 折线图

组合折线图包含了 AQI、降水量和风速的一年中的逐月数据，可以通过按钮切换不同年份。AQI 用柱状图的形式显示，并根据 AQI 值的范围使用不同的颜色表示，其大小被标注在柱子上。降水量和风速使用折线图显示，相应坐标轴分别位于图左右两侧。

鼠标悬浮在图中某个月份的对应区域，可以显示该月份的 AQI、降水量和风速的具体数值。

=== 平行坐标图

平行坐标图分为月平行坐标图和年平行坐标图两种。

月平行坐标图展示了一年中每个月的 AQI、降水量和风速四种数据，可用按钮切换年份。一条折线穿过月份、AQI、降水量和风速四个维度的坐标轴，表示该月份的 AQI、降水量和风速的具体数值，折线颜色根据 AQI 值的范围使用不同的颜色表示。

年平行坐标图展示了 2015-2021 年的 AQI、降水量、风速和工业能四种数据。数据粒度由月度转为年度，折线颜色依旧根据 AQI 值的范围使用不同的颜色表示。

鼠标悬浮在图中某个月份的折线上，可以在旁边显示该折线的具体数据。可以在坐标轴上用鼠标点击并拖动来选择显示某个范围内的折线，范围外的折线会被隐藏。

== 交互设计

=== 项目框架与侧边栏

#figure(
  image("assets/app.png", width: 50%),
  caption: [
    最外层框架
  ],
) <fig:app.vue>

项目框架是基于 Vue 和 Element Plus 构建的可视化应用主界面，整体采用左中右布局：左侧为可折叠的导航菜单，中部为地图组件，右侧包含图表组件。交互逻辑围绕导航菜单选择、地图点击事件以及图表显隐控制展开。

#figure(
     image("assets/el-menu.png", width: 50%),
     caption: [
         导航菜单
     ],
)<fig:el-menu>

左侧导航菜单通过 el-menu 组件实现，数据来源于 ChartData 数组，支持二级菜单结构，即主菜单为图表类型，子菜单为具体图表功能。默认选中 “首页” 菜单项，点击菜单项时触发 handleSelect 事件。菜单支持折叠功能，点击右侧的折叠按钮时，通过toggleCollapse 函数修改 isCollapse 状态，控制菜单的展开/折叠状态，提升界面空间利用率。子菜单通过 v-for 循环渲染，每个子菜单项对应不同的图表类型，如柱状图下的 AQI、降水等，为后续图表切换提供交互入口。

#figure(
     image("assets/components_in_app.png", width: 50%),
     caption: [
         图表组件
     ],
)<fig:components_in_app>

地图组件，绑定了 CityClick 事件，当用户点击地图上的城市时，会触发 handleCityClick 函数，将点击的城市名称如 “杭州市”赋值给响应式变量 selectedCity。这一变量作为桥梁，将地图点击事件的结果传递给其他图表组件如柱状图，玫瑰图等，他们均通过 CityName 属性接收 selectedCity，从而加载对应城市的数据。例如，点击地图上的 “北京市”，所有依赖 selectedCity 的图表会自动更新为北京市的数据。


#figure(
     image("assets/change_components.png", width: 50%),
     caption: [
         图表切换
     ],
)<fig:change_components>

右侧的图表组件通过v-if指令控制显隐，每个组件对应一个布尔型状态变量。这些状态变量需结合导航菜单的点击事件进行联动。例如，当用户在导航菜单中点击 “柱状图 - AQI” 时，需将columAQIshow 设 为true，其他图表的状态设为 false，以实现单一图表的显示。

=== 地图

#figure(
  image("assets/map.png", width: 50%),
  caption: [
    地图视图
  ],
)<fig:map.vue>

地图组件实现了一个可交互的中国地图可视化功能，用户可以通过点击按钮在两种配色模式间切换：省份配色模式下，同一省份的城市显示相同颜色；AQI 配色模式下，城市颜色根据其 AQI 数值动态变化。地图支持点击交互，点击任意城市会触发事件向上传递城市名称。

组件初始化时会执行以下操作：在onMounted生命周期钩子中，等待 DOM 渲染完成后调用mapEcharts函数初始化地图。该函数创建 ECharts 实例，注册中国地图数据，并配置地图样式、标题、提示框和视觉映射。地图数据通过 generateMapData 函数生成，初始状态下使用省份配色模式。

用户与地图的交互主要有两种方式：一是点击 "切换 AQI 配色" 按钮，这会触发toggleColorMode 函数，切换 useAQIColor 状态并调用 refreshMap 更新地图颜色；二是点击地图上的城市，这会触发 CityClick 事件，将被点击城市的名称传递给父组件。refreshMap 函数通过更新 ECharts 实例的配置来刷新地图数据，确保配色模式的切换即时生效。


=== 柱状图



组件通过 props 接收父组件传递的 CityName（默认 “杭州市”），并在 mounted 生命周期钩子中自动调用 fetchCityData 方法加载数据。该方法通过动态导入 JSON 文件获取对应城市的 AQI 、降水量、风速和工业能耗数据，并将原始数据中的字符串日期转换为 Date 对象存储在 dateObj 字段中。用户通过顶部的日期选择器 startDate 和 endDate 设置时间范围，两个输入框的 v-model 双向绑定响应式变量，当日期变更时触发 watch 监听函数 filterDataByDate，根据选择的日期范围过滤 data1 中的数据，生成 filteredData。



数据过滤完成后，drawChart 方法负责渲染 D3 柱状图，先清空图表容器内的所有元素，再根据容器的实际尺寸（通过getBoundingClientRect获取）计算内边距后的可用空间，创建 SVG 画布并定位到正确位置。X 轴使用scaleBand比例尺，以日期字符串为定义域，生成水平方向的分类轴，添加 padding 0.1 避免柱体紧贴边缘。Y 轴，使用scaleLinear比例尺，以 AQI 数值为定义域，范围从 0 到数据中的最大值，确保柱体高度随数据动态变化。通过data-join模式更新柱体，每个柱体的位置、宽度和高度由比例尺计算得出，填充颜色为固定的steelblue，并添加过渡动画。X 轴标签添加 45 度倾斜和右对齐样式，避免日期文本重叠；Y 轴添加垂直方向的 “AQI 值” 标签，提升可读性。



=== 热力图

热力图组件接收父组件传递的城市名称作为输入，通过 watch 监听城市名称变化。当城市名称改变时，会并行加载四个不同指标（AQI、降水量、风力、工业能耗）的历史数据文件，提取对应城市的时间序列数据后，使用皮尔逊相关系数公式计算各指标间的相关性矩阵。这个计算过程确保了数据的科学性和准确性，为后续的可视化提供了可靠基础。

组件提供了两个核心交互控件，显示模式选择器和色系选择器。用户可以通过下拉菜单选择完整矩阵、上三角或下三角的显示方式，这通过 filteredData 函数实现数据过滤，使热力图只展示用户需要的部分。同时，用户还能在发散色（红蓝）和顺序色（蓝白红）两种色系间切换，以适应不同的数据分析需求。这些选择通过 Vue 的响应式变量管理，任何变化都会触发updateChart函数重新渲染热力图。

图表本身集成了多种交互功能。鼠标悬停在热力图单元格上时，会显示详细的提示信息，包括两个指标名称和精确到两位小数的相关系数值，这通过 tooltip 配置实现。当窗口大小改变时，组件会自动调整图表尺寸以保持最佳显示效果，增强了在不同设备上的适应性。整个交互逻辑形成了一个闭环，从数据加载、计算、可视化到用户反馈，每个环节都紧密相连，确保用户能够直观、高效地探索不同城市环境指标间的相关性。

=== 玫瑰图


=== 折线图



=== 平行坐标图


= 案例展示