# 数据说明

1. `china.json` 为按照市组织的中国地图边界，数据来源为 [DataV](https://datav.aliyun.com/portal/school/atlas/area_selector)

2. `china1.json` 为按照省组织的中国地图边界

3. `city_to_province` 记录了城市到省份的映射关系。
4. `province_to_color` 记录了省份到颜色的映射关系。
5. `city_to_province1` 记录了包括没有数据的城市到省份的映射关系。
